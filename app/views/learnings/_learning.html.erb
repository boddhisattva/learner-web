<%= turbo_frame_tag learning do %>
<!-- Desktop/Tablet layout (3 columns) -->
<div class="is-hidden-mobile" style="display: grid; grid-template-columns: 60% 25% 15%; gap: 1rem; padding: 0.75rem 1rem; border-bottom: 1px solid #dbdbdb; align-items: center;">
  <div><%= learning.lesson %></div>
  <div><%= learning.created_at.strftime("%B %d, %Y") %></div>
  <div>
    <%= render Learnings::ActionsComponent.new(learning: learning) %>
  </div>
</div>

<!-- Mobile layout (2 columns) -->
<div class="is-hidden-tablet" style="display: grid; grid-template-columns: 70% 30%; gap: 1rem; padding: 0.75rem 1rem; border-bottom: 1px solid #dbdbdb; align-items: center;">
  <div>
    <%= learning.lesson %>
    <div><small class="has-text-grey"><%= learning.created_at.strftime("%B %d, %Y") %></small></div>
  </div>
  <div>
    <%= render Learnings::ActionsComponent.new(learning: learning) %>
  </div>
</div>
<% end %>
